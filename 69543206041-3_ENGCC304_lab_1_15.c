#include <stdio.h>

int main() {
    char in[50],po[50],out[50];
    
    scanf("%s %s %s", in, po, out);
    
    printf("Input: %s\nProcess: %s\nOutput: %s\n", in, po, out);
   
    return 0;
}